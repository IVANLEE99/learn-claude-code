# Claude Code 新手入门版

## 1. 先理解它是干什么的
你可以把 Claude Code 理解成：

- 在终端里工作的 AI 编程助手
- 能帮你看代码、改代码、解释报错、运行命令、整理 git 改动
- 适合做：
  - 读项目
  - 修 bug
  - 写小功能
  - 改文档
  - 分析报错

## 2. 最常见的使用场景

### 场景一：刚接手一个项目
你可以直接问：

- `这个项目是做什么的？`
- `帮我梳理一下目录结构`
- `核心入口文件在哪？`
- `登录逻辑在哪几个文件？`

### 场景二：看不懂一段代码
你可以说：

- `解释一下 src/index.js`
- `这段函数在做什么？`
- `这段代码为什么要这么写？`

### 场景三：修 bug
你可以说：

- `帮我排查这个报错`
- `修复这个空指针异常`
- `分析为什么接口会返回 500`

### 场景四：直接改代码
你可以说：

- `把按钮文字改成“提交中”`
- `给这个函数加参数校验`
- `新增一个导出 JSON 的功能`

## 3. 新手最推荐的提问方式

直接按这个结构提问就很好：

```text
目标：你想做什么
文件：涉及哪些文件（如果知道）
约束：不要改什么 / 只做最小改动 / 不要重构
补充：报错信息、日志、截图、复现步骤
```

例如：

```text
目标：修复登录后不跳转的问题
文件：src/login.js 和 src/router.js
约束：只做最小改动，不要重构
补充：控制台报错 Cannot read property 'push' of undefined
```

## 4. 第一次用时，建议这样和它配合

### 第一步：先让它看懂项目
你可以输入：

- `先帮我看一下这个项目结构`
- `告诉我入口文件和核心模块`
- `这个仓库主要功能是什么`

### 第二步：锁定目标
例如：

- `我现在要改登录功能，先帮我找到相关文件`
- `我现在只想修这个报错，不要做别的改动`

### 第三步：先分析，再动手
这是最稳的方式：

- `先分析原因，不要改代码`
- `给我一个最小修改方案`
- `确认后再直接改`

### 第四步：改完再检查
例如：

- `改完后帮我检查有没有明显问题`
- `帮我看看这次修改影响了哪些地方`
- `帮我总结一下改了什么`

## 5. 最适合新手的 10 个常用提示词

你可以直接复制用：

### 理解项目
1. `帮我快速理解这个项目`
2. `这个项目的核心代码在哪`
3. `这个目录下面每个文件是做什么的`

### 理解代码
4. `解释一下这个文件`
5. `解释这个函数的执行流程`
6. `这段代码为什么会报错`

### 修改代码
7. `帮我用最小改动修复这个问题`
8. `只改这个文件，不要动其他地方`
9. `先给方案，不要直接修改`

### 收尾检查
10. `总结一下你刚刚改了什么`

## 6. 新手最容易踩的坑

### 坑 1：提问太模糊
比如：

- `帮我改一下`
- `这个有问题`
- `优化一下`

这样信息太少，Claude Code 很难准确执行。

更好的是：

- `把登录按钮点击后改成 loading 状态`
- `修复接口超时后页面一直转圈的问题`
- `优化这个函数，但不要改返回结构`

### 坑 2：一口气塞太多需求
比如一句话里同时说：

- 修 bug
- 改 UI
- 加测试
- 顺便重构

这样容易失控。

更推荐拆成几步：

1. `先找出 bug 原因`
2. `再修复`
3. `最后看要不要补测试`

### 坑 3：不说明边界
如果你只想做小修改，最好直接说：

- `只做最小改动`
- `不要重构`
- `不要新增文件`
- `不要改接口结构`

这样结果会更符合预期。

## 7. 一个实战示例

假设你发现某个 JS 文件有 bug。

你可以这样一步步问：

### 第 1 步：先看懂
`解释一下 src/index.js`

### 第 2 步：指出问题
`这个文件运行时报错，帮我分析原因，但先不要改代码`

### 第 3 步：要求方案
`给我一个最小修改方案`

### 第 4 步：开始修改
`按这个方案直接改`

### 第 5 步：让它总结
`总结一下这次改动`

这套流程非常适合新手。

## 8. 什么时候该让 Claude Code 先分析，不要直接改
建议先说“不要改代码”的情况：

- 你还没搞清楚项目结构
- 你不确定 bug 根因
- 你怕它改太多
- 你想先确认方案

可直接说：

- `先分析，不要修改`
- `先告诉我会改哪些文件`
- `先给我方案，我确认后再动手`

## 9. 什么时候可以直接让它改
如果需求很明确，可以直接说：

- `把 src/index.js 改成输出当前时间`
- `把按钮颜色改成蓝色`
- `把这个函数改成 async/await`
- `修复这里的空值判断`

## 10. 新手最推荐的工作习惯

### 推荐习惯 1：先小步操作
不要一开始就说“重构整个项目”。

先从小任务开始：

- 看一个文件
- 修一个 bug
- 改一个按钮
- 加一个接口

### 推荐习惯 2：总是加限制条件
比如：

- `最小改动`
- `不要影响其他模块`
- `不要新增依赖`
- `不要创建新文件`

### 推荐习惯 3：改完让它复盘
比如：

- `解释一下你为什么这么改`
- `这次改动可能影响哪里`
- `帮我总结修改点`

这样进步很快。

## 11. 给新手的一套万能模板
以后你几乎可以一直用这个：

```text
我想做的事：
[写目标]

相关文件：
[写文件名，不知道可以不写]

限制条件：
[例如：最小改动、不要重构、不要新增文件]

补充信息：
[报错、日志、复现步骤]
```

例如：

```text
我想做的事：
修复首页白屏问题

相关文件：
src/app.js

限制条件：
只做最小改动，不要重构

补充信息：
打开页面后控制台报 undefined is not a function
```

## 12. 你下一步可以怎么学
如果你想继续，我建议按这个顺序：

1. 学会怎么提需求
2. 学会先分析再修改
3. 学会限制修改范围
4. 学会让它总结改动
